# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.22.0
httpx>=0.26.0
python-multipart>=0.0.6
requests>=2.31.0
slowapi>=0.1.9
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ── Environment & Config ───────────────────────────────────────────────────────
python-dotenv>=1.0.0
PyYAML>=6.0.1

# ── System Observer & Cross-Platform Utilities ─────────────────────────────────
psutil>=5.9.0
Pillow>=10.2.0

# ── Voice / Speech Synthesis ───────────────────────────────────────────────────
edge-tts>=6.1.9
elevenlabs>=1.0.0

# ── AI / ML Models ─────────────────────────────────────────────────────────────
groq>=0.4.0
google-genai>=0.1.0

# ── Database & Memory Persistence ──────────────────────────────────────────────
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# ── Testing & Code Quality ─────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
black>=23.12.0
flake8>=7.0.0

# ── Vector Search & RAG ────────────────────────────────────────────────────────
langchain-text-splitters>=0.2.0
langchain-groq>=0.1.0
langchain-community>=0.2.0
langchain-core>=0.2.0
# ── Web Scraping & Agent Browser ───────────────────────────────────────────────
beautifulsoup4>=4.12.0
tavily-python>=0.3.0
# ── Data Processing ────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0


# ── Telegram Bot ──────────────────────────────────────────────────────────────
python-telegram-bot>=20.7

# ── Background Monitor & News Search (DDG fallback) ───────────────────────────
duckduckgo-search>=6.0.0

# ── Plugins & External APIs ───────────────────────────────────────────────────
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
APScheduler>=3.10.0
dateparser>=1.2.0
qrcode>=7.4.0

# ── Relay Proxy WAF Bypass ────────────────────────────────────────────────────
curl_cffi>=0.5.9# Bruno dependencies.
# Grows one step at a time so every addition is deliberate and reviewable.
# Install with:  pip install -r requirements.txt
#
# This is the CPU build, and it is what most users run. Owners of an NVIDIA
# GPU can additionally install requirements-gpu.txt for faster, more accurate
# speech recognition; Bruno detects it at startup and needs no configuration.

python-dotenv==1.0.1

# Audio capture and playback
sounddevice==0.5.5
numpy==2.5.1

# Speech-to-text
faster-whisper==1.2.1

# Language model access. Used for every OpenAI-compatible provider, not just
# OpenAI itself.
openai==2.52.0

# System tray
pystray==0.19.5
pillow==12.3.0
