# Shared Layer

This module contains shared types, constants, and enumerations used across the `jarvis_os` foundation layers.
This prevents circular dependencies between `identity`, `memory`, and `brain`.
